{% extends 'AppBundle::app-container.html.twig' %}

{% block view %}

    {%
        set columns = [
            {'label': 'Nombre', 'name': 'nombre'},
            {'label': 'Descripción', 'name': 'descripcion'},
            {'label': 'Activo', 'name': 'activo', 'type': 'boolean'}
        ]
    %}

    {%
        set actions = [
            {'label': 'Detalles', 'icon': 'fa fa-list-alt', 'path': 'categoria_show' },
            {'label': 'Modificar', 'icon': 'fa fa-pencil', 'path': 'categoria_edit' }
        ]
    %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <strong class="card-title">Categorías</strong>
                </div>
                <div class="card-body">
                    <div class="pb-2">
                        <a href="{{ path('categoria_new') }}" class="btn text-primary"><i class="fa fa-plus"></i>&nbsp; Adicionar</a>
                    </div>

                    {% include 'AppBundle::components/table.html.twig' with {'columns': columns, 'items': categorias, 'actions': actions}%}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
